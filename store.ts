import { createSelector } from 'reselect';

// reducer => state, action => nextState
// vanilla selector => state => a piece of state
// reselect selector

// complete source of truth of the UI

// reducers => methods that manage changes
// actions => instructions for change
// selectors => fetching data from the store
